<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.mygellery.MainActivity" > 
     <Button 
        android:id="@+id/alpha"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="AlphaAnimation(透明度动画)"
        />
      <Button 
        android:id="@+id/scale"
        android:layout_below="@id/alpha"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="ScaleAnimation(缩放动画)"
        />
      <Button 
        android:id="@+id/rotate"
        android:layout_below="@id/scale"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="ScaleAnimation(旋转动画)"
        />
      <Button 
        android:id="@+id/translater"
        android:layout_below="@id/rotate"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="ScaleAnimation(位移动画)"
        />
          <ImageView 
        android:id="@+id/imageview"
         android:layout_below="@id/translater"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"/>
</RelativeLayout>
